---
description: Update .kb/ to reflect changes in a given corpus path
argument-hint: path within corpus (required)
---

Invoke the `kb-lint` skill for path: `$ARGUMENTS`

If `$ARGUMENTS` is empty, ask the user which path to lint. Do not default to the whole corpus.
