---
description: Query the .kb/ knowledge base using progressive disclosure
argument-hint: question in natural language
---

Invoke the `kb-query` skill with this question: `$ARGUMENTS`

If `$ARGUMENTS` is empty, ask the user for the question before proceeding.
