---
description: Check whether .kb/ is stale — read-only, returns re-lint candidates
---

Invoke the `kb-check-drift` skill on the current corpus. This is a read-only operation — it will not modify `.kb/` or `.kb-state.json`.
