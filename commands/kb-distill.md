---
description: Extract a decision record from a completed plan into .kb/decisions/
argument-hint: path to completed plan file or directory
---

Invoke the `kb-distill` skill for plan: `$ARGUMENTS`

If `$ARGUMENTS` is empty, ask the user which plan to distill. Do not default to the most recent plan — distill intent must come from the user.
